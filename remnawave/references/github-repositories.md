# GitHub Repositories

Источник:

- `https://github.com/remnawave`

## Что видно на официальной GitHub organization page

По состоянию на `2026-06-11` официальная organization page показывает, что домен `docs.rw` верифицирован для организации `remnawave`.

Среди публично видимых репозиториев на странице перечислены:

- `panel`
- `backend`
- `frontend`
- `templates`
- `node`
- `subscription-page`
- `xtls-sdk`
- `migrate`
- `python-sdk`
- `asn-index`

## Практический смысл

- Panel-related install flows опираются на `backend` и Panel docs.
- Node workflows связаны с `node`.
- Subscription Page опирается на `subscription-page`.
- Xray-related automation в экосистеме Remnawave также использует `xtls-sdk`.

Если нужен полный список из organization beyond what is visible on the public page snapshot, перепроверять сам `https://github.com/remnawave` перед изменениями.
